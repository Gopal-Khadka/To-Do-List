<!DOCTYPE html>
<html lang="en">
  <head>
    <title>To Do List</title>
    <link rel="stylesheet" href="css/styles.css" />
  </head>
  <body>
    <div class="box" id="heading">
      <h1><%= day %> To Do List</h1>
    </div>
    <div class="box">
      <% for (let i = 0; i < tasks.length; i++) { %> <% if(tasks[i]!==""){ %>
      <div class="item">
        <input type="checkbox" />
        <p><%= tasks[i]%></p>
      </div>
      <% } %> <% } %>

      <form action="/" method="post">
        <input
          type="text"
          name="task"
          id="myInput"
          autocomplete="off"
          placeholder="New Item"
        />
        <button type="submit">+</button>
      </form>
    </div>
    <script>
      window.onload = function () {
        document.getElementById("myInput").focus();
      };
    </script>
  </body>
</html>
